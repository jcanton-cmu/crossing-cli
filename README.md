# Crossing-CLI
Just run the exe and you can add a `-h` flag to anything to get instructions.

## Dynamic script configuration
[example config script](./generators.json)
It's read on runtime meaning it doesn't require recompilation to load.